#include <bits/stdc++.h>
using namespace std;
#define el "\n"

int k, m;
string str;

int main(){
    freopen(".\\txt\\PW.INP", "r", stdin);
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    cin>>k>>m;
    cin>>str;
    for(int i = 0; i < k;i++){
        int l, r;
        cin>>l>>r;
    }

    return 0;
}