#include <bits/stdc++.h>
#define el "\n"
using namespace std;
using ll = long long;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    ll n, k;
    cin>>n>>k;

    cout<< 2 * n / (k + 2)<<el;
    
    return 0;
}