#include <bits/stdc++.h>
using namespace std;
#define el "\n"

int a, b, x, y;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    cin>>a>>b>>x>>y;
    cout<<a * x + b * y;

    return 0;
}