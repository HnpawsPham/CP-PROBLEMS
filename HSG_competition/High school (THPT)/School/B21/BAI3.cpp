#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define el "\n"

int n;
ll k;
const int maxn = (int)1e5 + 5;
int a[maxn];
vector<string> s;

int main(){
    freopen(".\\txt\\BAI3.INP", "r", stdin);
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    cin>>n>>k;
    for(int i = 1; i <= n; i++) cin>>a[i];


    return 0;
}