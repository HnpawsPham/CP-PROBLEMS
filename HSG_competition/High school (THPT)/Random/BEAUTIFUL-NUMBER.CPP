#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define el "\n"

ll n;
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    cin>>n;
    return cout<<1LL * n * 18, 0;
}